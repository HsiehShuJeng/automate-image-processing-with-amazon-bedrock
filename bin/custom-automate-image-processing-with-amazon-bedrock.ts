#!/usr/bin/env node
/**
 * Maintains backwards compatibility with the original entry point name.
 * The actual application bootstrap logic lives in {@link ./image-processing-app}.
 */
import './image-processing-app';
